# Миграция картинок в public/

## Что изменилось?

**Проблема**: При `yarn build` картинки из `src/assets/pictures/` не копировались в выходную папку.

**Решение**: Картинки перемещены в `public/pictures/` - стандартный подход Vite для статических ресурсов.

## Сделанные изменения

### ✅ 1. Картинки перемещены
- **Было**: `src/assets/pictures/`
- **Стало**: `public/pictures/`

### ✅ 2. Компонент обновлён
`src/components/WordImage.tsx`:
```tsx
// Было
src={`/src/assets/pictures/${picture}`}

// Стало
src={`/pictures/${picture}`}
```

### ✅ 3. Документация обновлена
- `README.md` - обновлён путь к картинкам
- `WORD_FORMAT.md` - обновлены инструкции
- `CHANGELOG_PICTURES.md` - добавлено описание исправления
- `public/pictures/README.md` - создана инструкция для папки

### ✅ 4. Старая папка удалена
`src/assets/pictures/` больше не используется и удалена.

## Как это работает

### Dev-режим (`yarn dev`)
```
http://localhost:5173/pictures/table.svg ✅
```

### Production (`yarn build`)
```
public/pictures/table.svg → dist/pictures/table.svg ✅
                          → docs/pictures/table.svg ✅
```

Vite **автоматически** копирует всё содержимое папки `public/` в выходную папку при сборке.

## Проверка

Чтобы убедиться, что всё работает:

```bash
# Сборка
yarn build

# Проверьте, что картинки скопировались
ls -la docs/pictures/

# Должны увидеть:
# carpet.png
# pillow.png
# refrigerator.png
# table.svg
# towel.png
```

## Добавление новых картинок

**Теперь**:
1. Кладите файлы в `public/pictures/`
2. Используйте в `words.json`:
```json
{
  "id": "word-id",
  "picture": "filename.svg",
  ...
}
```

Никаких дополнительных настроек не требуется! 🎉

## Почему это лучше?

✅ **Работает в production** - Vite автоматически копирует файлы  
✅ **Стандартный подход** - именно для этого нужна папка `public/`  
✅ **Проще в использовании** - не нужно настраивать сборку  
✅ **Быстрее** - статические файлы обслуживаются напрямую  
✅ **Поддержка любых форматов** - SVG, PNG, JPG, WebP и т.д.

## Откат (если нужно)

Если по какой-то причине нужно вернуться назад:

```bash
# Восстановить папку
mkdir -p src/assets/pictures
cp public/pictures/* src/assets/pictures/

# Вернуть путь в WordImage.tsx
src={`/src/assets/pictures/${picture}`}
```

Но это **не рекомендуется**, так как не будет работать в production! ⚠️

